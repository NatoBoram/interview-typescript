/**
 * 1. Make a `GET` request using `fetch` to obtain a person from <https://swapi.dev>
 * 2. Type the response appropriately
 * 3. Put that person's hair color in `hairColor`
 * 4. Return the person's hair color
 *
 * @param id The ID of the person to fetch
 * @see https://swapi.dev
 */
export async function getPeople(id: number) {
	// Replace the entire function with your answer.
	id
	return Promise.resolve("blond")
}

export let hairColor: string
